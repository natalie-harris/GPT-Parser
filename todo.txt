pipeline class needs primary folder to save dfs to when there is no custom folder specified
save file is <df_name>_<date and time>.csv
need to make sure that we save the csvs in a way that can be properly read (properly escape commas, backslashes)

use threading module to parallelize module processing